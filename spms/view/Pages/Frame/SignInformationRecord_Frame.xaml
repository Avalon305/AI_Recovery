<Page x:Class="spms.view.Pages.Frame.SignInformationRecord_Frame"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:spms.view.Pages.Frame"
             
             mc:Ignorable="d" d:DesignWidth="948" Height="321.666">
    <Grid>
        <WrapPanel Orientation="Vertical">
            <Grid Margin="0,8,0,0">
                <Border Background="Silver" Margin="135,3,570.2,-5" BorderBrush="Black" BorderThickness="1,1,0,0">
                    <Label HorizontalAlignment="Center" Margin="-0.8,-2.8,-1.6,-3.6" VerticalAlignment="Center" Height="24" Width="212" RenderTransformOrigin="0.562,0.656" HorizontalContentAlignment="Center">康复前</Label>
                </Border>
                <Border Height="20" Background="Silver" Margin="376,3,330.2,-5" BorderBrush="Black" BorderThickness="1,1,1,0">
                    <Label HorizontalAlignment="Left" Margin="90.2,-3.8,0,-4.6" VerticalAlignment="Center" Height="26" Width="56">康复后</Label>
                </Border>
            </Grid>


            <DataGrid x:Name="SignInformationRecord" RowHeaderWidth="0" SelectionMode="Single" HorizontalAlignment="Left" Margin="5,5,0,0" VerticalAlignment="Top"  GridLinesVisibility="None"  Height="280" Width="942" CanUserSortColumns="False" CanUserResizeColumns="False" CanUserReorderColumns="False"  AutoGenerateColumns="False" IsReadOnly="true" >

                <!--选中行失去焦点时的颜色-->
                <DataGrid.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="White"/>

                </DataGrid.Resources>
                <!--固定行高，取消拉伸-->
                <DataGrid.RowStyle>

                    <Style TargetType="DataGridRow">
                        <Setter Property="Height" Value="18" />
                    </Style>
                </DataGrid.RowStyle>
                <!--去掉选中单元格的框线-->
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell" >
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="BorderThickness" Value="0"/>
                            </Trigger>
                        </Style.Triggers>
                        <!--单元格tooltip效果-->
                        <Setter Property="ToolTipService.ToolTip"  Value="{Binding RelativeSource={RelativeSource Mode=Self},Path=Content.Text}" />
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Create,StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" Header="训练日期" Width="130"/>
                    <DataGridTextColumn Binding="{Binding Pre_Pressure}" Header="血压" Width="60"/>
                    <DataGridTextColumn Binding="{Binding Pre_Pulse}" Header="脉搏" Width="80"/>
                    <DataGridTextColumn Binding="{Binding Pre_HeartRate}"  Header="心率" Width="50"/>
                    <DataGridTextColumn Binding="{Binding Pre_AnimalHeat}" Header="体温" Width="50"/>
                    <DataGridTextColumn Binding="{Binding Suf_Pressure}"  Header="血压" Width="60"/>
                    <DataGridTextColumn Binding="{Binding Suf_Pulse}" Header="脉搏" Width="80"/>
                    <DataGridTextColumn Binding="{Binding Suf_HeartRate}"  Header="心率" Width="50"/>
                    <DataGridTextColumn Binding="{Binding Suf_AnimalHeat}" Header="体温" Width="50"/>
                    <DataGridTextColumn Binding="{Binding WaterInput}" Header="水分摄取" Width="60"/>
                    <!--<DataGridTextColumn Binding="{Binding Inquiry}" Header="问诊确认单"  Width="80">
</DataGridTextColumn>-->
                    <DataGridTemplateColumn Header="问诊确认单" Width="100" MaxWidth="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <!--设置当单元格内容超过最大长度时，显示...-->
                                <TextBlock Text="{Binding Inquiry}" TextTrimming="WordEllipsis"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellStyle>
                            <Style TargetType="DataGridCell" >
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </Trigger>
                                </Style.Triggers>
                                <!--加tooltip，显示单元格内容-->
                                <Setter Property="ToolTipService.ToolTip" Value="{Binding Inquiry}"/>
                            </Style>
                        </DataGridTemplateColumn.CellStyle>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Binding="{Binding Join}" Header="参加/不参加" Width="80"/>
                    <!--<DataGridTextColumn Binding="{Binding CareInfo}" Header="看护记录" Width="150"/>-->
                    <DataGridTemplateColumn Header="看护记录" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <!--设置当单元格内容超过最大长度时，显示...-->
                                <TextBlock Text="{Binding CareInfo}" TextTrimming="WordEllipsis"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                        <DataGridTemplateColumn.CellStyle>
                            <Style TargetType="DataGridCell" >
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="BorderThickness" Value="0"/>
                                    </Trigger>
                                </Style.Triggers>
                                <!--加tooltip，显示单元格内容-->
                                <Setter Property="ToolTipService.ToolTip" Value="{Binding CareInfo}"/>
                            </Style>
                        </DataGridTemplateColumn.CellStyle>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </WrapPanel>
        <!--<TabControl Name="TabControl1" HorizontalAlignment="Left" Height="337" Margin="0,20,0,0" VerticalAlignment="Top" Width="572">
            <TabItem Header="原始窗口">
                <Grid Background="#FFE5E5E5">
                    <TextBox HorizontalAlignment="Left"  Height="305" Margin="0" TextWrapping="Wrap" Text="" VerticalAlignment="Top" Width="562"/>


                </Grid>
            </TabItem>
        </TabControl>-->
    </Grid>
</Page>
